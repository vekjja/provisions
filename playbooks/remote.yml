# EMN DevOps initializer

- name: Provisions Remote Environment
  hosts: remote windows
  gather_facts: true
  roles:
    - { role: packages, tags: ['packages'] }
    - { role: system, tags: ['system'] }
    - { role: files, tags: ['files'] }
